import styled from 'styled-components'
import { Colors, FontTypes } from '~/Themes'

export const AntText = styled.span`
  font-family: ${FontTypes.InterRegular};
  font-weight: 400;
  font-size: 16px;
  color: ${Colors.primary};
  letter-spacing: -0.02em;
`
